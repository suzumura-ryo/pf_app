<%= form_for @procedure url: procedures_path(@procedure) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "レシピを書いてください" %>
  </div>
  <%= f.submit "登録する", class: "btn btn-primary" %>
<% end %>